# <ModuleName> Public Interfaces

## Purpose
One paragraph describing the module scope and the intent of its public APIs.

## Usage Patterns
- Short, actionable guidance for common usage flows.

## Public Header Index
- `<module>/<rel>.hpp` — one-line purpose.

## Header Details
### <module>/<rel>.hpp
Location: `engine/<module>/source/<module>/<rel>.hpp`
Include: `#include <<module>/<rel>.hpp>`

Purpose: One sentence describing what this header provides.

Key Types: `TypeA`, `TypeB`
Key APIs: `Class::method()`, `freeFunction()`, `MACRO_NAME`

Usage Notes:
- One or two bullets on how to use it safely.

Used By: `moduleA`, `moduleB`
